{
  "product": "PulseGuard PC",
  "version": "0.6.40",
  "summary": "PulseGuard v0.6.40 is a safety release: elevated helper scripts no longer run from user-writable folders, updates ask before installing and support release signing, Fix & Optimize no longer cleans the registry, wipes Quick Access pins or closes your apps, startup changes use the Task Manager method and can be undone run by run, and restore points, WinGet retry holds and report arrays now report the truth.",
  "sections": [
    {
      "heading": "Elevated helpers moved to a protected folder",
      "detail": "Scripts, specs and installers that PulseGuard runs as administrator now live in C:\\ProgramData\\PulseGuard PC Elevated, which only Administrators and SYSTEM can change. The Software Protection SYSTEM-task fallback and its Windows permission edits were removed. Apps closed by Game Ready are relaunched unelevated. Daily scheduled scans are refused (and removed) while PulseGuard is installed in a folder standard programs can modify."
    },
    {
      "heading": "Updates ask first and can be signed",
      "detail": "The updater now asks before installing, stages downloads in the protected folder, and verifies an RSA release signature once a public key is pinned in PulseGuard_Updater.ps1."
    },
    {
      "heading": "Fix & Optimize no longer removes your data",
      "detail": "Registry cleaning is review-only, the Privacy Sweep is manual and keeps Quick Access pins and Jump Lists, PATH is never edited automatically, Deep Clean keeps browser/thumbnail caches and resumable downloads, and background-app closing is manual with Spotify, Teams, Creative Cloud and Epic removed from the close list."
    },
    {
      "heading": "Startup changes and Restore fixed",
      "detail": "Run-key startup items are disabled with Task Manager's StartupApproved flag so they can be re-enabled from Task Manager. Restore now steps back one run at a time and restores registry values with their original type."
    },
    {
      "heading": "Truthful results",
      "detail": "Restore points are verified before being reported as created, the WinGet helper defines every function it uses and fails safe on its retry-hold file, report lists stay JSON arrays, serial numbers are redacted from the report, and two calls to functions that did not exist were fixed."
    },
    {
      "heading": "Stability-aware advice",
      "detail": "Several different BSOD codes now point to RAM/EXPO/CPU tuning first, memory running below its rated speed is treated as a possible stability test while crashes are active, and a BSOD is no longer counted twice through its Kernel-Power reboot."
    }
  ]
}
